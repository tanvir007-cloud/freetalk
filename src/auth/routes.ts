export const authRoutes = ["/login", "/signin"];

export const publicRoutes = ["/", "/profile/:path*","/post/:path*"];

export const apiAuthPrefix = "/api";

export const DEFAULT_LOGIN_REDIRECT = "/home";
